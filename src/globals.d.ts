/**
 * Global math constants available throughout the codebase.
 * Import '@/globals' to ensure runtime values are initialized.
 */

/** Alias for Math.PI */
declare var PI: number;

/** Alias for 2 * Math.PI (full circle in radians) */
declare var TWO_PI: number;
